crud operation with file system :
although we hv already did most of CRUD operations ( CREATE a file, READ a file, UPDATE a file, DELETE a file ),but we gona do again :-
also we gonna renaming a file.

